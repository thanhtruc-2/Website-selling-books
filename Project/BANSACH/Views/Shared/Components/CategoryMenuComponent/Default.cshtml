@*@using TECH.Areas.Admin.Models
@model List<CategoryModelView>
@if (Model != null && Model.Count > 0)
{

    <a href="#" id="dropdown04" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" class="nav-link dropdown-toggle">
        Danh mục
    </a>
    <div aria-labelledby="dropdown04" class="dropdown-menu">
        @foreach (var item in Model)
        {
            <a href="/san-pham/@item.id" class="dropdown-item">@item.name</a>
        }

    </div>

}
*@

@using TECH.Areas.Admin.Models
@model List<CategoryModelView>
@if (Model != null && Model.Count > 0)
{
    <li>
    <div class="megamenu-wrapper not-index">
        <div class="megamenu-head">
            <i class="fas fa-bars"></i> Danh mục sản phẩm
        </div>
        <div class="megamenu-body">
            <ul class="no-bullets" style="text-align: left;">
                    @foreach (var item in Model)
                    {
                        <li class="li-dropdown">
                            <a href="/san-pham/@item.id">@item.name</a>
                        </li>
                    }
                @*<li class="">
                    <a href="/collections/van-phong-pham-qua-tang-2023">Đồ Trang Trí - Lưu Niệm </a>
                </li>
                <li class="">
                    <a href="/collections/van-phong-pham-qua-tang-2023">Gợi Ý Quà Tặng </a>
                </li>*@
            </ul>
        </div>
    </div>
    </li>
    @*<ul class="ul-nmn">
        @foreach (var item in Model)
        {
            <li>

                <a href="/san-pham/@item.id" style="color:black;font-weight:bold">
                    @if (!string.IsNullOrEmpty(item.icon))
                    {
                        <img loading="lazy" class="icon-mn" src="/category-image/@item.icon" alt="@item.name" style="width: 50px;height:50px">
                    }
                    else
                    {
                        <img loading="lazy" class="icon-mn" src="/category-image/hoa-khai-truong500.png" alt="@item.name" style="width: 50px;height:50px">
                    }

                    <br>
                    @item.name
                </a>
            </li>
        }

    </ul>*@

}



